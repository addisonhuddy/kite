# copy to ./kite.properties (or ~/.config/kite/kite.properties)
# Works against the TESTING.md docker-compose harness: gen-tls.sh,
# `docker compose up -d`, docker-init.sh. PLAINTEXT listener on :9092.
bootstrap.servers=localhost:9092
security.protocol=PLAINTEXT
